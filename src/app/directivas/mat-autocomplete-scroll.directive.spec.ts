import { MatAutocompleteScrollDirective } from './mat-autocomplete-scroll.directive';

describe('MatAutocompleteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MatAutocompleteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
