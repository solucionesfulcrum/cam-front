import { EstiloInputDirective } from './estilo-input.directive';

describe('EstiloInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloInputDirective();
    expect(directive).toBeTruthy();
  });
});
