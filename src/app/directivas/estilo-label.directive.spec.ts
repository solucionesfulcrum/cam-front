import { EstiloLabelDirective } from './estilo-label.directive';

describe('EstiloLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new EstiloLabelDirective();
    expect(directive).toBeTruthy();
  });
});
